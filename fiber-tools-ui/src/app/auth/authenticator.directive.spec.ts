import { AuthenticatorDirective } from './authenticator.directive';

describe('AuthenticatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthenticatorDirective();
    expect(directive).toBeTruthy();
  });
});
